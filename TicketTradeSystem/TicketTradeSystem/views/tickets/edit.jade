extends ../layout

block content
    h1 #{ticket._id}
        form(role='form',action='/tickets/#{ticket.id}/edit',method='post')
            p Amount: 
                input(type='text', value='#{ticket.amount}', name='amount')
            p Price: 
                input(type='number', value='#{ticket.price}', name='price')
            p 
                button(type='submit') Update