extends ../layout

block content
    h1=title
        a.btn.btn-primary(href="/tickets/new", style="margin-bottom: 10px; margin-left: 10px;") <i class="fa fa-plus"></i> 
    .row
        h2 eventslist
        p
            #{eventslist}
        h2 ticketslist
        p
            #{ticketslist}